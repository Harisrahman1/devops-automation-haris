<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ app_title }}</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; margin: 0; padding: 0; }
    .wrap { max-width: 900px; margin: 0 auto; padding: 48px 24px; }
    .card { border: 1px solid #ddd; border-radius: 16px; padding: 24px; }
    h1 { margin: 0 0 12px 0; font-size: 28px; }
    p  { margin: 0 0 10px 0; line-height: 1.5; }
    code { background: #f4f4f4; padding: 2px 6px; border-radius: 6px; }
    .meta { margin-top: 18px; font-size: 14px; color: #555; }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h1>{{ app_title }}</h1>
      <p><strong>Status:</strong> Live</p>
      <p>{{ app_message }}</p>
      <p class="meta">
        Host: <code>{{ inventory_hostname }}</code><br/>
        Deployed: <code>{{ ansible_date_time.iso8601 }}</code>
      </p>
    </div>
  </div>
</body>
</html>
